# DR10 Real-Image Lens Finder: Implementation Package

Runnable scaffold to build DR10 cutouts (positives, negatives, contaminants), train baseline and domain-specialized
models (ResNet, EfficientNet), and produce evaluation artifacts for a selection-function / failure-modes paper.

See requirements.txt and scripts under data/, training/, evaluation/, inference/, paper/.
